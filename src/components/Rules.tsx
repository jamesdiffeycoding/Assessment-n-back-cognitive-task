export default function Rules() {
  return (
    <div className="rules-section">
      <h2>Rules</h2>
      <p className="rules-section-info">
        Click the gray game box below when you see the same letter as the letter
        two letters before. E.g. the sequence "hello" has no matches, while in
        the sequence "ababab" every letter after the second is a match.
      </p>
    </div>
  );
}
